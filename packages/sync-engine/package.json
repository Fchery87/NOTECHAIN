{
  "name": "@notechain/sync-engine",
  "version": "0.1.0",
  "private": true,
  "description": "CRDT-based sync logic for NoteChain web application",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "start:ws": "bun --env-file=../../apps/web/.env.local run src/server/index.ts"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.0",
    "@supabase/supabase-js": "^2.47.0",
    "uuid": "^11.0.5",
    "dexie": "^4.0.11",
    "eventemitter3": "^5.0.1"
  },
  "devDependencies": {
    "@types/uuid": "^10.0.0",
    "typescript": "^5.7.3",
    "@types/bun": "latest"
  }
}
